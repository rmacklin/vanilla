<div class="card">
  <%= link_to image_tag(image.url), image %>

  <ul class="tag-list">
    <% image.tags.each do |tag| %>
        <li class="tag"><a href="#"><%= tag %></a></li>
    <% end %>
  </ul>
</div>
