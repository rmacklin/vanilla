<p id="notice"><%= notice %></p>

<h1>Images</h1>

<div class="image_list">
  <% @images.each do |image| %>
    <%= link_to render(partial: 'image', locals: { image: image }), image %>
  <% end %>
</div>

<%= link_to 'New Image', new_image_path %>
