<div class="card">
  <%= link_to image_tag(image.url), image %>

  <ul class="tag-list">
    <% image.tags.each do |tag| %>
        <li class="tag"><%= link_to tag, tag_path(tag.name) %></li>
    <% end %>
  </ul>
</div>
